# Survey Results

Each CSV in this folder is one team member's submission to the AI ordering channel survey.

Filenames follow the pattern: `<ISO timestamp>__<sanitized email>.csv`.

To review responses, just download the CSVs from this folder.
